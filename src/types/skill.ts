export interface SkillDTO {
    skill: string;
    level: string;
  }
  
  export interface Profile {
    name: string;
    title: string;
    email: string;
    mobile: string;
    website: string;
    bio: string;
    address: string;
  }